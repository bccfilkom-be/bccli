package repository

import(
    {{ .Module }}
)

type {{ .Domain }}{{ .Database }}Itf interface {}

type {{ .Domain }}{{ .Database }} struct {
    db *{{ .DBDriver }}
}

func New{{ .Domain }}{{ .Database }}(db *{{ .DBDriver }}) {{ .Domain }}{{ .Database }}Itf {
    return &{{ .Domain }}{{ .Database }}{db}
}
